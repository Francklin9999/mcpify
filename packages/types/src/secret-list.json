{
  "$comment": "CANONICAL cross-language secret-list. See docs/04-legal-modes.md. TS (extension) imports this file; Python (scraper) reads this EXACT file. Header names matched case-insensitively; fieldPatterns are glob (* = any) matched case-insensitively against body field names. Do not fork — one source of truth.",
  "headers": [
    "authorization",
    "cookie",
    "set-cookie",
    "x-api-key",
    "x-auth-token",
    "proxy-authorization"
  ],
  "fieldPatterns": [
    "*token*",
    "*secret*",
    "*password*",
    "*session*"
  ]
}
