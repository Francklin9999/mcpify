{
  "name": "@mcp/forge-mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "mcp-forge": "dist/src/main.js"
  },
  "scripts": {
    "build": "npm run build --workspace=@mcp/types && tsc -p tsconfig.json",
    "start": "node dist/src/main.js",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "dependencies": {
    "@mcp/types": "0.0.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
